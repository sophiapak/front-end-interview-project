import styled from 'styled-components';

const IconWrapper = styled.div`
  margin: 40px auto 0 auto;
  display: flex;
`;

const SocialMediaIcons = styled.img`
  margin: 0 12px;
`;

export { IconWrapper, SocialMediaIcons };
